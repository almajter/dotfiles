-- Plugins live in this directory, one file per plugin, each returning a spec.
-- lazy.nvim imports every file here automatically — no central list to update.
-- Empty for now; step 3 adds the first one.
return {}
